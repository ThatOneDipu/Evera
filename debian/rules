#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_configure:
	meson setup build --prefix=/usr

override_dh_auto_build:
	ninja -C build

override_dh_auto_install:
	DESTDIR=$$(pwd)/debian/evera meson install -C build

override_dh_strip:
	true
